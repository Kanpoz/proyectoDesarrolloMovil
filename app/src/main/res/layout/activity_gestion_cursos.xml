<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootGestionCursos"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="25dp"
    android:background="@color/background_light"
    tools:context=".roles.admin.GestionCursosActivity">

    <!-- Título -->
    <TextView
        android:id="@+id/tvTituloCursos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GESTIÓN DE CURSOS"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Botón NUEVO CURSO + -->
    <Button
        android:id="@+id/btnNuevoCurso"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="NUEVO CURSO +"
        android:textAllCaps="false"
        android:backgroundTint="@color/AzulInstitucional"
        android:textColor="@android:color/white"
        android:layout_marginEnd="16dp"
        app:layout_constraintTop_toTopOf="@id/tvTituloCursos"
        app:layout_constraintBottom_toBottomOf="@id/tvTituloCursos"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Encabezado de columnas -->
    <LinearLayout
        android:layout_marginTop="16dp"
        android:id="@+id/layoutHeaderCursos"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvTituloCursos"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="3"
            android:text="Curso"
            android:textStyle="bold"
            android:textColor="@color/black" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="Profesor"
            android:textStyle="bold"
            android:textColor="@color/black" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="Materia"
            android:textStyle="bold"
            android:textColor="@color/black" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Acciones"
            android:textStyle="bold"
            android:textColor="@color/black" />
    </LinearLayout>

    <!-- Lista de cursos -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCursosAdmin"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/layoutHeaderCursos"
        app:layout_constraintBottom_toTopOf="@id/btnActualizarCursos"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@layout/item_curso_admin" />

    <!-- Botón ACTUALIZAR (abajo) -->
    <Button
        android:id="@+id/btnActualizarCursos"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="80dp"
        android:text="ACTUALIZAR LISTA"
        android:backgroundTint="@color/AzulInstitucional"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
